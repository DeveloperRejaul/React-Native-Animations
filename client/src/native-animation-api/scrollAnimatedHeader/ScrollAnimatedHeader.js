import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function ScrollAnimatedHeader() {
  return (
    <View>
      <Text>ScrollAnimatedHeader</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
