import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function HeaderSearchBar() {
  return (
    <View>
      <Text>HeaderSearchBar</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
